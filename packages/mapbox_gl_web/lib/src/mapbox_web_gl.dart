library mapbox_gl_web;

import 'package:flutter_web_plugins/flutter_web_plugins.dart';

class MapboxMapPlugin {
  static void registerWith(Registrar registrar) {
    // Nothing here; the real implementation will be provided by the mapbox_gl package at runtime.
    // This stub exists to satisfy pub resolution when using a patched dependency.
  }
}
